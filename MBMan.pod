
=head1 NAME
 
    MBMan - Eine IMAP Mailboxmanagement API in Perl.

=head1 ANWENDUNG

    use MBMan;
    use Data::Dumper;

    my $mbman = MBMan->new(
        Debug     => 0,
        Ssl       => 1,
        Peek      => 1,
        Uid       => 1,
        Directory => '~/MBData'
    );

    $mbman->connect( Server => 'server.tld' );

    $mbman->login(
        User => 'user@domain.tld',
        Password => 'pa$$w0rd'
    );

    my $sinfo = $mbman->get_server_info();

    print Dumper ($sinfo);

    my $minfo = $mbman->get_messages_info(
        Modus => 'Full',
        HashEnv => 1,
        DecodeMime => 1
    );

    print Dumper ($minfo);

    $mbman->logout();
